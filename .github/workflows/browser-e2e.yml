name: Browser CI

on:
  push:
    branches: [main]
    paths:
      - 'packages/browser/**'
      - 'examples/wasm/**'
      - 'src/**'
      - '.github/workflows/browser-e2e.yml'
  pull_request:
    branches: [main]
    paths:
      - 'packages/browser/**'
      - 'examples/wasm/**'
      - 'src/**'
      - '.github/workflows/browser-e2e.yml'

jobs:
  # Unit tests (run in Node.js, no browser needed)
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        working-directory: packages/browser
        run: npm install

      - name: Build with esbuild
        working-directory: packages/browser
        run: node esbuild.config.js

      - name: Install Playwright browsers
        working-directory: packages/browser
        run: npx playwright install chromium

      - name: Run unit tests
        working-directory: packages/browser
        run: npm test

  # E2E tests (requires browser) - uses Playwright container
  e2e:
    runs-on: ubuntu-latest
    needs: unit-tests
    container:
      image: mcr.microsoft.com/playwright:v1.52.0-noble
    steps:
      - uses: actions/checkout@v4

      - name: Setup Zig
        run: |
          apt-get update && apt-get install -y xz-utils
          curl -L https://ziglang.org/download/0.15.2/zig-linux-x86_64-0.15.2.tar.xz | tar -xJ
          echo "$PWD/zig-linux-x86_64-0.15.2" >> $GITHUB_PATH

      - name: Build WASM module
        run: zig-linux-x86_64-0.15.2/zig build wasm

      - name: Copy WASM to examples
        run: cp zig-out/bin/lanceql.wasm examples/wasm/

      - name: Install dependencies
        working-directory: packages/browser
        run: npm install

      - name: Run E2E tests
        working-directory: packages/browser
        run: npx playwright test --project=chromium

      - name: Generate Benchmark Summary
        if: always()
        working-directory: packages/browser
        run: |
          echo "## 🏆 LanceQL vs DuckDB WASM Benchmark" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -f benchmark-results.json ]; then
            echo "### Performance Comparison" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # Parse and format results using Node.js
            node -e "
              const fs = require('fs');
              const results = JSON.parse(fs.readFileSync('benchmark-results.json'));
              
              // Group by row count
              const byRows = {};
              for (const entry of results) {
                byRows[entry.rowCount] = entry.results;
              }
              
              // Output tables for each scale
              for (const [rows, benchmarks] of Object.entries(byRows).sort((a,b) => a[0] - b[0])) {
                const scale = rows >= 100000 ? '100K' : rows >= 10000 ? '10K' : '1K';
                console.log('#### ' + scale + ' Rows');
                console.log('');
                console.log('| Query Type | LanceQL (ms) | DuckDB (ms) | Ratio | Winner |');
                console.log('|------------|--------------|-------------|-------|--------|');
                
                let lanceWins = 0;
                for (const b of benchmarks) {
                  const winner = parseFloat(b.ratio) < 1 ? '**LanceQL** 🏆' : 'DuckDB';
                  if (parseFloat(b.ratio) < 1) lanceWins++;
                  console.log('| ' + b.query + ' | ' + b.lanceql + ' | ' + b.duckdb + ' | ' + b.ratio + ' | ' + winner + ' |');
                }
                console.log('');
                
                if (lanceWins === benchmarks.length) {
                  console.log('> **Total Victory at ' + scale + ' scale!** 🎉');
                  console.log('');
                }
              }
              
              // Overall summary
              const allBenchmarks = results.flatMap(r => r.results);
              const lanceWinCount = allBenchmarks.filter(b => parseFloat(b.ratio) < 1).length;
              const total = allBenchmarks.length;
              
              console.log('### Overall Score');
              console.log('');
              console.log('**LanceQL wins ' + lanceWinCount + '/' + total + ' benchmarks** (' + Math.round(lanceWinCount/total*100) + '%)');
              if (lanceWinCount === total) {
                console.log('');
                console.log('🏆 **TOTAL VICTORY: LanceQL beats DuckDB WASM at every scale for every query type!**');
              }
            " >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ Benchmark results not available" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "*Commit: $GITHUB_SHA*" >> $GITHUB_STEP_SUMMARY

      - name: Upload Benchmark Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: benchmark-results
          path: packages/browser/benchmark-results.json

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: packages/browser/playwright-report/
          retention-days: 7
